---
title: Constantes predefinidas
source_url: https://www.php.net/manual/es/tidy.constants.php
source_repo: https://github.com/php/doc-es.git
source_ref: master
source_commit: 954a0d911
source_path: reference/tidy/constants.xml
technology: php
version: master
license: CC-BY-3.0
retrieved_at: '2026-08-02'
section: tidy
translation_status: ready
translation_revision: 82142fe99
order: 93880
---

## Constantes predefinidas

Estas constantes son definidas por esta extensión, y solo están disponibles si esta extensión ha sido compilada con PHP, o bien cargada en tiempo de ejecución.

Cada `TIDY_TAG_*` representa una etiqueta HTML. Por ejemplo, `TIDY_TAG_A` representa una etiqueta `"<a href="XX">link</a>"`.

`TIDY_TAG_UNKNOWN` (`int`)  

`TIDY_TAG_A` (`int`)  

`TIDY_TAG_ABBR` (`int`)  

`TIDY_TAG_ACRONYM` (`int`)  

`TIDY_TAG_ADDRESS` (`int`)  

`TIDY_TAG_ALIGN` (`int`)  

`TIDY_TAG_APPLET` (`int`)  

`TIDY_TAG_AREA` (`int`)  

`TIDY_TAG_ARTICLE` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_ASIDE` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_AUDIO` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_B` (`int`)  

`TIDY_TAG_BASE` (`int`)  

`TIDY_TAG_BASEFONT` (`int`)  

`TIDY_TAG_BDI` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_BDO` (`int`)  

`TIDY_TAG_BGSOUND` (`int`)  

`TIDY_TAG_BIG` (`int`)  

`TIDY_TAG_BLINK` (`int`)  

`TIDY_TAG_BLOCKQUOTE` (`int`)  

`TIDY_TAG_BODY` (`int`)  

`TIDY_TAG_BR` (`int`)  

`TIDY_TAG_BUTTON` (`int`)  

`TIDY_TAG_CANVAS` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_CAPTION` (`int`)  

`TIDY_TAG_CENTER` (`int`)  

`TIDY_TAG_CITE` (`int`)  

`TIDY_TAG_CODE` (`int`)  

`TIDY_TAG_COL` (`int`)  

`TIDY_TAG_COLGROUP` (`int`)  

`TIDY_TAG_COMMAND` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_COMMENT` (`int`)  

`TIDY_TAG_DATALIST` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_DD` (`int`)  

`TIDY_TAG_DEL` (`int`)  

`TIDY_TAG_DETAILS` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_DFN` (`int`)  

`TIDY_TAG_DIALOG` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_DIR` (`int`)  

`TIDY_TAG_DIV` (`int`)  

`TIDY_TAG_DL` (`int`)  

`TIDY_TAG_DT` (`int`)  

`TIDY_TAG_EM` (`int`)  

`TIDY_TAG_EMBED` (`int`)  

`TIDY_TAG_FIELDSET` (`int`)  

`TIDY_TAG_FIGCAPTION` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_FIGURE` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_FONT` (`int`)  

`TIDY_TAG_FOOTER` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_FORM` (`int`)  

`TIDY_TAG_FRAME` (`int`)  

`TIDY_TAG_FRAMESET` (`int`)  

`TIDY_TAG_H1` (`int`)  

`TIDY_TAG_H2` (`int`)  

`TIDY_TAG_H3` (`int`)  

`TIDY_TAG_H4` (`int`)  

`TIDY_TAG_H5` (`int`)  

`TIDY_TAG_H6` (`int`)  

`TIDY_TAG_HEAD` (`int`)  

`TIDY_TAG_HEADER` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_HGROUP` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_HR` (`int`)  

`TIDY_TAG_HTML` (`int`)  

`TIDY_TAG_I` (`int`)  

`TIDY_TAG_IFRAME` (`int`)  

`TIDY_TAG_ILAYER` (`int`)  

`TIDY_TAG_IMG` (`int`)  

`TIDY_TAG_INPUT` (`int`)  

`TIDY_TAG_INS` (`int`)  

`TIDY_TAG_ISINDEX` (`int`)  

`TIDY_TAG_KBD` (`int`)  

`TIDY_TAG_KEYGEN` (`int`)  

`TIDY_TAG_LABEL` (`int`)  

`TIDY_TAG_LAYER` (`int`)  

`TIDY_TAG_LEGEND` (`int`)  

`TIDY_TAG_LI` (`int`)  

`TIDY_TAG_LINK` (`int`)  

`TIDY_TAG_LISTING` (`int`)  

`TIDY_TAG_MAIN` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_MAP` (`int`)  

`TIDY_TAG_MARK` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_MARQUEE` (`int`)  

`TIDY_TAG_MENU` (`int`)  

`TIDY_TAG_MENUITEM` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_META` (`int`)  

`TIDY_TAG_METER` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_MULTICOL` (`int`)  

`TIDY_TAG_NAV` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_NOBR` (`int`)  

`TIDY_TAG_NOEMBED` (`int`)  

`TIDY_TAG_NOFRAMES` (`int`)  

`TIDY_TAG_NOLAYER` (`int`)  

`TIDY_TAG_NOSAVE` (`int`)  

`TIDY_TAG_NOSCRIPT` (`int`)  

`TIDY_TAG_OBJECT` (`int`)  

`TIDY_TAG_OL` (`int`)  

`TIDY_TAG_OPTGROUP` (`int`)  

`TIDY_TAG_OPTION` (`int`)  

`TIDY_TAG_OUTPUT` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_P` (`int`)  

`TIDY_TAG_PARAM` (`int`)  

`TIDY_TAG_PLAINTEXT` (`int`)  

`TIDY_TAG_PRE` (`int`)  

`TIDY_TAG_PROGRESS` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_Q` (`int`)  

`TIDY_TAG_RB` (`int`)  

`TIDY_TAG_RBC` (`int`)  

`TIDY_TAG_RP` (`int`)  

`TIDY_TAG_RT` (`int`)  

`TIDY_TAG_RTC` (`int`)  

`TIDY_TAG_RUBY` (`int`)  

`TIDY_TAG_S` (`int`)  

`TIDY_TAG_SAMP` (`int`)  

`TIDY_TAG_SCRIPT` (`int`)  

`TIDY_TAG_SECTION` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_SELECT` (`int`)  

`TIDY_TAG_SERVER` (`int`)  

`TIDY_TAG_SERVLET` (`int`)  

`TIDY_TAG_SMALL` (`int`)  

`TIDY_TAG_SOURCE` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_SPACER` (`int`)  

`TIDY_TAG_SPAN` (`int`)  

`TIDY_TAG_STRIKE` (`int`)  

`TIDY_TAG_STRONG` (`int`)  

`TIDY_TAG_STYLE` (`int`)  

`TIDY_TAG_SUB` (`int`)  

`TIDY_TAG_SUMMARY` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_SUP` (`int`)  

`TIDY_TAG_TABLE` (`int`)  

`TIDY_TAG_TBODY` (`int`)  

`TIDY_TAG_TD` (`int`)  

`TIDY_TAG_TEMPLATE` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_TEXTAREA` (`int`)  

`TIDY_TAG_TFOOT` (`int`)  

`TIDY_TAG_TH` (`int`)  

`TIDY_TAG_THEAD` (`int`)  

`TIDY_TAG_TIME` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_TITLE` (`int`)  

`TIDY_TAG_TR` (`int`)  

`TIDY_TAG_TRACK` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_TT` (`int`)  

`TIDY_TAG_U` (`int`)  

`TIDY_TAG_UL` (`int`)  

`TIDY_TAG_VAR` (`int`)  

`TIDY_TAG_VIDEO` (`int`)  
Añadido en libtidy 5.0.0. Disponible a partir de PHP 7.4.0.

`TIDY_TAG_WBR` (`int`)  

`TIDY_TAG_XMP` (`int`)  

<!-- -->

`TIDY_NODETYPE_ROOT` (`int`)  
nodo raíz

`TIDY_NODETYPE_DOCTYPE` (`int`)  
tipo de documento

`TIDY_NODETYPE_COMMENT` (`int`)  
comentario HTML

`TIDY_NODETYPE_PROCINS` (`int`)  
Instrucción de procesamiento

`TIDY_NODETYPE_TEXT` (`int`)  
Texto

`TIDY_NODETYPE_START` (`int`)  
etiqueta de apertura

`TIDY_NODETYPE_END` (`int`)  
etiqueta de cierre

`TIDY_NODETYPE_STARTEND` (`int`)  
etiqueta vacía

`TIDY_NODETYPE_CDATA` (`int`)  
CDATA

`TIDY_NODETYPE_SECTION` (`int`)  
Sección XML

`TIDY_NODETYPE_ASP` (`int`)  
Código ASP

`TIDY_NODETYPE_JSTE` (`int`)  
Código JSTE

`TIDY_NODETYPE_PHP` (`int`)  
Código PHP

`TIDY_NODETYPE_XMLDECL` (`int`)  
Declaración XML
