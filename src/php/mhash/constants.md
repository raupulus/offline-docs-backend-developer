---
title: Constantes predefinidas
source_url: https://www.php.net/manual/es/mhash.constants.php
source_repo: https://github.com/php/doc-es.git
source_ref: master
source_commit: 954a0d911
source_path: reference/mhash/constants.xml
technology: php
version: master
license: CC-BY-3.0
retrieved_at: '2026-08-02'
section: mhash
translation_status: ready
translation_reviewed: false
translation_revision: c929e6755
order: 46910
---

## Constantes predefinidas

Estas constantes son definidas por esta extensión, y solo están disponibles si esta extensión ha sido compilada con PHP, o bien cargada en tiempo de ejecución.

A continuación se presenta una lista de los modos que son soportados por mhash. Si un modo no está listado aquí, pero está listado en la documentación mhash como soportado, se puede considerar que esta lista no está actualizada.

`MHASH_ADLER32` (`int`)  

`MHASH_CRC32` (`int`)  

`MHASH_CRC32B` (`int`)  

`MHASH_CRC32C` (`int`)  
Disponible a partir de PHP 7.4.0.

`MHASH_FNV132` (`int`)  

`MHASH_FNV1A32` (`int`)  

`MHASH_FNV164` (`int`)  

`MHASH_FNV1A64` (`int`)  

`MHASH_GOST` (`int`)  

`MHASH_HAVAL128` (`int`)  

`MHASH_HAVAL160` (`int`)  

`MHASH_HAVAL192` (`int`)  

`MHASH_HAVAL224` (`int`)  

`MHASH_HAVAL256` (`int`)  

`MHASH_JOAAT` (`int`)  

`MHASH_MD2` (`int`)  

`MHASH_MD4` (`int`)  

`MHASH_MD5` (`int`)  

`MHASH_MURMUR3A` (`int`)  
Disponible desde PHP 8.1.0.

`MHASH_MURMUR3C` (`int`)  
Disponible desde PHP 8.1.0.

`MHASH_MURMUR3F` (`int`)  
Disponible desde PHP 8.1.0.

`MHASH_RIPEMD128` (`int`)  

`MHASH_RIPEMD160` (`int`)  

`MHASH_RIPEMD256` (`int`)  

`MHASH_RIPEMD320` (`int`)  

`MHASH_SHA1` (`int`)  

`MHASH_SHA224` (`int`)  

`MHASH_SHA256` (`int`)  

`MHASH_SHA384` (`int`)  

`MHASH_SHA512` (`int`)  

`MHASH_SNEFRU128` (`int`)  

`MHASH_SNEFRU256` (`int`)  

`MHASH_TIGER` (`int`)  

`MHASH_TIGER128` (`int`)  

`MHASH_TIGER160` (`int`)  

`MHASH_WHIRLPOOL` (`int`)
