---
title: Constantes predefinidas
source_url: https://www.php.net/manual/es/openal.constants.php
source_repo: https://github.com/php/doc-es.git
source_ref: master
source_commit: 954a0d911
source_path: reference/openal/constants.xml
technology: php
version: master
license: CC-BY-3.0
retrieved_at: '2026-08-02'
section: openal
translation_status: ready
translation_reviewed: false
translation_revision: 86e6094e8
order: 58690
---

## Constantes predefinidas

Estas constantes son definidas por esta extensión, y solo están disponibles si esta extensión ha sido compilada con PHP, o bien cargada en tiempo de ejecución.

`ALC_FREQUENCY` (`int`)  
Atributo de contexto

`ALC_REFRESH` (`int`)  
Atributo de contexto

`ALC_SYNC` (`int`)  
Atributo de contexto

`AL_FREQUENCY` (`int`)  
Configuración del buffer

`AL_BITS` (`int`)  
Configuración del buffer

`AL_CHANNELS` (`int`)  
Configuración del buffer

`AL_SIZE` (`int`)  
Configuración del buffer

`AL_BUFFER` (`int`)  
Configuración de la fuente/escucha (Entero)

`AL_SOURCE_RELATIVE` (`int`)  
Configuración de la fuente/escucha (Entero)

`AL_SOURCE_STATE` (`int`)  
Configuración de la fuente/escucha (Entero)

`AL_PITCH` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_GAIN` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_MIN_GAIN` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_MAX_GAIN` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_MAX_DISTANCE` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_ROLLOFF_FACTOR` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_CONE_OUTER_GAIN` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_CONE_INNER_ANGLE` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_CONE_OUTER_ANGLE` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_REFERENCE_DISTANCE` (`int`)  
Configuración de la fuente/escucha (Número de punto flotante)

`AL_POSITION` (`int`)  
Configuración de la fuente/escucha (Número vectorial de punto flotante)

`AL_VELOCITY` (`int`)  
Configuración de la fuente/escucha (Número vectorial de punto flotante)

`AL_DIRECTION` (`int`)  
Configuración de la fuente/escucha (Número vectorial de punto flotante)

`AL_ORIENTATION` (`int`)  
Configuración de la fuente/escucha (Número vectorial de punto flotante)

`AL_FORMAT_MONO8` (`int`)  
Formato PCM

`AL_FORMAT_MONO16` (`int`)  
Formato PCM

`AL_FORMAT_STEREO8` (`int`)  
Formato PCM

`AL_FORMAT_STEREO16` (`int`)  
Formato PCM

`AL_INITIAL` (`int`)  
Estado de la Fuente

`AL_PLAYING` (`int`)  
Estado de la Fuente

`AL_PAUSED` (`int`)  
Estado de la Fuente

`AL_STOPPED` (`int`)  
Estado de la Fuente

`AL_LOOPING` (`int`)  
Estado de la Fuente

`AL_TRUE` (`int`)  
Valor booleano reconocido por OpenAL

`AL_FALSE` (`int`)  
Valor booleano reconocido por OpenAL
