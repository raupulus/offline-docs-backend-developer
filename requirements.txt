# Dependencias Python del pipeline.
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Ver info/dependencias.md para las del sistema (git, pandoc, texinfo).

pyyaml>=6.0        # sources.yaml y el front-matter de cada documento
markdown>=3.5      # markdown -> HTML en el generador de sitio
jinja2>=3.1        # plantillas del visor
pygments>=2.17     # resaltado de sintaxis en tiempo de compilación
